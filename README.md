Currently WIP. Fully type safe implementation of a hyperliquid SDK for golang. Built to be fully compatible with go-ethereum and optimizes for performance and ergonomics

Multisig is handled with a separate request which is signed before the multisig request is made. This is done for ease of use
